package com.nbawatchability.app.data

/**
 * Points at the deployed backend.
 *
 * TODO: replace with the real Render URL once the service is live, e.g.
 * "https://nba-watchability-backend.onrender.com" (no trailing slash).
 */
const val BACKEND_BASE_URL = "TODO_SET_RENDER_URL"
